# Binary-Brain Backend Configuration
PORT=5000
NODE_ENV=development

# JWT Secret
JWT_SECRET=binary_brain_super_secure_jwt_secret_2026

# PostgreSQL Database URL (Optional: In-memory store used if omitted)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/binary_brain_db

# Client CORS Origin
CLIENT_ORIGIN=http://localhost:3000
